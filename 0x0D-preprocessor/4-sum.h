#ifndef SUM_H
#define SUM_H

/**
 * Desc - function-like macro SUM(x, y) that computes
 *        the sum of the numbers x and y
 * @x: input integer
 * @y: input integer
 */

#define SUM(x, y) ((x) + (y))

#endif
